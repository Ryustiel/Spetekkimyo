"""
A FastAPI app that exposes a button to regenerate the font file, 
that imports the font file, 
exposes an input field to type demo text, 
and exposes many input fields with predefined text for watching how things fit together.
"""

from spetekkimyo import generate_font

generate_font()
